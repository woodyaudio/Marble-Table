## 배경

라이브 사이트 지도가 CARTO 정책 변경(무료 타일도 API 키 필요)으로 "API KEY REQUIRED" 워터마크와 함께 깨진 상태였습니다. 장기적으로 도메인 구매 후 광고를 붙여 상용화할 계획이라, OSM 순정 타일(상용/대량 트래픽 비권장, 예고 없이 차단 가능)로 임시 땜빵하는 대신 **네이버 지도 API v3로 구조를 확정**했습니다.

## 변경 사항

- **index.html**: Leaflet CDN 태그 제거 → 네이버 지도 스크립트로 교체 (`ncpKeyId`는 발급 전까지 플레이스홀더 `YOUR_NCP_CLIENT_ID`)
- **map-tab.html**: Leaflet(`L.*`) API를 `naver.maps.*` API로 전면 교체 (지도/마커/지하철 노선·역 라벨/테마). 지도 테마는 3종(기본/라이트/다크) → 2종(기본/다크, CSS 필터)으로 단순화
- **contact-tab.html**: 위치 검색을 Nominatim(OSM) → 네이버 Geocoder로 교체. 존재하지 않는 엘리먼트를 참조해 설정 화면 진입 시 매번 예외를 던지던 죽은 코드 제거, 앱 테마-지도 테마 동기화 인덱스 수정
- **README.md**: 기술 스택/데이터 구조 최신화, 네이버 지도 API 키 발급 안내 추가
- **.claude/launch.json**: 로컬 프리뷰용 정적 서버 설정 추가

## 남은 작업 (병합 전 필수)

- [x] 네이버 클라우드 플랫폼에서 Client ID 발급 — Application "matjeom" 등록 완료 (Dynamic Map + Geocoding 활성화, Service URL: `https://woodyaudio.github.io`)
- [x] `index.html`의 `ncpKeyId`를 실제 Client ID(`3z36hqrgla`)로 교체
- [ ] 병합 후 실제 GitHub Pages 배포본에서 지도가 정상 렌더링되는지 최종 확인 (로컬 `localhost`는 등록된 도메인이 아니라 인증 실패가 정상 — 콘솔 Service URL에 localhost 추가하면 로컬 테스트도 가능)

## 검증

- 플레이스홀더 키 상태로 로컬 서버에서 확인: xlsx 파싱/식당 목록/마커 생성/지하철 토글/GPS/설정 화면 다크모드 동기화 모두 새로운 JS 오류 없이 동작
- 실제 Client ID 반영 후: 콘솔 에러 메시지에 올바른 Client ID가 찍히는 것 확인, 남은 401은 도메인 미등록(localhost) 때문임을 확인 — 등록된 도메인(GitHub Pages)에서는 정상 작동 예상
- 실측 무료 이용량(등록 계정 기준): Geocoding 월 3,000,000회, Dynamic Map 월 6,000,000회 — 이 프로젝트 트래픽 대비 충분히 여유 있음
